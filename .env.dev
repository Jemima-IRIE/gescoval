APP_ENV=development
APP_PORT=8080

# Database for local development
DB_HOST=db
DB_PORT=3306
DB_DATABASE=gescoval
DB_USER=root
DB_PASSWORD=ges_co_val

# MySQL root password for Docker Compose
MYSQL_ROOT_PASSWORD=ges_co_val

# PhpMyAdmin port
PMA_PORT=8081

# Session configuration
SESSION_LIFETIME=3600
SESSION_DOMAIN=localhost

# SMTP for local development (MailHog or similar)
SMTP_HOST=mailhog
SMTP_PORT=1025
SMTP_AUTH=false
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=
MAIL_FROM_ADDRESS=no-reply@gescoval.dev
MAIL_FROM_NAME=Gescoval